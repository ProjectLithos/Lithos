# OESDK 0.17.15

- Fixed generated kernel examples to print `OesdkBootContext.AbiVersion` with `%u`.
- Removed the invalid string-plus-integer pattern that performs pointer arithmetic.
- Updated Visual Studio templates, command-line project generation, API examples, and installer-source mirrors.
- Added `VerifyTemplateFormatting.py` to prevent this warning from returning.
