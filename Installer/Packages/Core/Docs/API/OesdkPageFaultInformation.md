# OesdkPageFaultInformation

Contains the faulting virtual address, raw error code, and decoded Boolean fields for present/protection state, read or write, supervisor or user access, reserved-bit violation, instruction fetch, protection-key violation, shadow-stack access, and SGX-related faults.
