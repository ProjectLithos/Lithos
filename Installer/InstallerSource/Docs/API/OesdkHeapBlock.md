# OesdkHeapBlock

Metadata preceding each permanent-heap block. It contains a validation magic value, payload size, free state, and previous/next links. Callers must treat block headers as allocator-owned.
