# OESDK 0.17.8

- Corrected public header version metadata.
- `VERSION`, `manifest.json`, Visual Studio metadata, and every packaged `kernel.h` now report the same SDK version.
- Added `Tests/VerifyVersionMetadata.py` to prevent stale header versions from being packaged again.
